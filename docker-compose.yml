version: '3.7'

services:
  pure3dapp:
    image: pure3dapp:${dockertag}
    container_name: pure3dapp
    hostname: pure3dapp
    environment:
      SECRET_FILE: ${SECRET_FILE}
      DATA_DIR: ${DATA_DIR}
    entrypoint: ./start.sh test
    ports:
      - "8000:8000"
